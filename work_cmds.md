# 工作常用常用指令笔记

## 1. 文本搜索与处理 (grep)

| 功能 | 命令示例 |
| :--- | :--- |
| **常用搜索重定向** | `grep -i "字符串" "文件" > "日志"` |
| **搜索指定后缀单词** | `grep -oP '\b\w*tc\b' your_file.sv` |

---

## 2. 文件与权限操作

### 2.1 统计磁盘空间 (du)

| 功能 | 命令 |
| :--- | :--- |
| 查看当前目录下所有大小 | `du -sh *` |
| 查看指定文件夹总大小 | `du -sh "文件夹"` |
| 查看当前目录隐藏文件夹大小 | `du -sh .??*` |

### 2.2 权限管理 (chmod)

| 功能 | 命令 |
| :--- | :--- |
| 为当前用户添加执行权限 | `chmod u+x "文件名"` |
| 删除当前用户的执行权限 | `chmod u-x "文件名"` |

---

## 3. 远程传输 (scp)

| 传输方向 | 命令示例 |
| :--- | :--- |
| **从远程下载到本地** | `scp -r [user@]host:[remote] [local]` |
| **从本地上传到远程** | `scp -r [local] [user@]host:[remote]` |

> **提示**：如果传输的是单个文件，可以不加 `-r` 参数。

---

## 4. Git 版本管理

| 类别 | 功能描述 | 常用指令 |
| :--- | :--- | :--- |
| **放弃修改** | 放弃当前目录下所有更改 | `git checkout -- .` |
| | 放弃特定文件的更改 | `git checkout -- [文件名]` |
| **强制回退** | 回退到与远程分支一致 | `git reset --hard origin/[分支名]` |
| | 回退到上一个版本 | `git reset --hard HEAD^` |
| | 强制回退到指定 Commit | `git reset --hard [版本号]` |
| **同步恢复** | 更新分支记录 (不改代码) | `git fetch origin` |
| | 从远程恢复特定文件 | `git checkout origin/main [文件]` |
| **分支管理** | 查看本地分支 | `git branch` |
| | 创建并切换新分支 | `git checkout -b [新分支名]` |
| | 合并指定分支到当前 | `git merge [分支名]` |
| | 只合并特定文件 | `git checkout [分支名] [文件]` |
| | 修改分支名称 | `git branch -m [旧名] [新名]` |
| | 删除远程分支 | `git push origin --delete [分支名]` |
| | 推送所有本地分支 | `git push origin --all` |
| **日志状态** | 查看工作区状态 | `git status` |
| | 精简查看最近 N 条记录 | `git log --oneline -n [N]` |
| | 查看文件具体修改 | `git diff [文件名]` |
| | 查看两分支间文件差异 | `git diff --name-only [B1] [B2]` |
| | 查看两分支间文件状态 | `git diff --name-status [B1] [B2]` |
| **高级配置** | 修正中文文件名乱码 | `git config --global core.quotepath false` |
| | 忽略 Office 临时文件 (以 ~$ 开头) | 在 `.gitignore` 中添加 `~$*` |
| **暂存管理** | 存入暂存栈 | `git stash` |
| (Stash) | 查看暂存列表 | `git stash list` |
| | 恢复并删除记录 | `git stash pop` |
| | 恢复并保留记录 | `git stash apply` |

---

## 5. VNC 服务管理

| 模块 | 功能 | 指令 |
| :--- | :--- | :--- |
| **服务启停** | 开启 VNC (需指定分辨率) | `vncserver -geometry 1920x1080 :1` |
| | 关闭 VNC 服务 | `vncserver -kill :1` |
| **防火墙** | 开启 5901 端口 | `sudo firewall-cmd --permanent...` |
| | 重新加载防火墙 | `sudo firewall-cmd --reload` |
| | 检查规则是否生效 | `sudo firewall-cmd --zone=public...` |



## 6. GVim 进阶技巧 (批量加文本)

| 方法 | 模式 | 操作步骤 |
| :--- | :--- | :--- |
| **方法 A** | 命令模式 | `gg` -> `:.,$s/$/[内容]/` |
| **方法 B** | 可视块模式 | `gg` -> `Ctrl-V` -> `G` -> `$` -> `A` -> 输入 -> `Esc` |

---
> *Generated by Antigravity*
